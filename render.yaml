services:
  - type: web
    name: wavedesigner-api
    runtime: docker
    region: oregon
    plan: starter
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ENVIRONMENT
        value: production
      - key: USE_DATABASE
        value: true